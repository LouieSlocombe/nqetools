cd /home/louie/skunkworks/nqetools/tests/orca
conda activate ipi_env2

# 0) Make the files executable (once):
chmod +x ase_orca_client.py

# 1) Start the i-PI server (runs in background here):
i-pi ipi_orca.xml > ipi.log 2>&1 &

# 2) Start the ORCA client – one per replica (just one here):
python ase_orca_client.py > client.log 2>&1 &
# (Make sure $ORCA_PATH and/or the orca binary are in your PATH)


"write me input files to run a simple md using i-pi and orca using the ASE client"